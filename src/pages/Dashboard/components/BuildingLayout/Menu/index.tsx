import { Building } from "../../../../../interfaces/building.interface";

export default function Menu({ building }: { building: Building }) {
    return (
        <div className="w-auto h-[70%] bg-black rounded text-white">Druhy menu</div>
    )
}
